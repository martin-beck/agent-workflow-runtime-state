Wire the hosted authority client behind the same mandatory Coordinator/AWQ/AWG/UI gate interface and prove the complete gate sequence with deterministic hosted-shaped endpoints. Add an explicit sandbox execution profile to AgentSession, contractor, and CLI admission; sandbox-required mode must fail closed when Bubblewrap or equivalent controls are unavailable, while the fake CI profile remains clearly labeled. Test rejection, stale/crossed outcomes, sandbox escape/network/process/resource cases, and end-to-end contractor execution through both adapters. Update release and umbrella compatibility only after exact-head CI and no-network qualification pass.
