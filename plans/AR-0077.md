# AR-0077 executable scheduler-to-contractor workflow

Build the integration façade and end-to-end local test scenarios. Test normal
completion, provider failure, host loss, cancellation, quality rejection,
bounded revision, oracle escalation, UI approval, stale lease, restart,
accounting mismatch, and artifact handoff. Keep live authorities represented by
validated fakes and explicit `not_performed` boundaries.
