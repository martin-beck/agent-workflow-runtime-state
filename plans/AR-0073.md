# AR-0073 adapter process transport harness

Build one transport implementation and one normalized event decoder behind the
AR-0063 protocol. Test malformed frames, overlong output, slow producers,
unexpected exit, signal races, unsupported capabilities, stale bindings,
replay, redaction, and bounded cancellation using local fake processes.
