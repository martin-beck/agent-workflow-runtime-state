# AR-0074 Codex-compatible executable adapter

Keep provider-specific parsing and capability quirks inside the adapter.
Require the shared transport, common conformance corpus, hostile traces, and
replay equivalence. A passing fake adapter is not evidence of real provider
availability.
