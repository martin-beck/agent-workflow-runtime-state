Implement a host capability probe and an enforceable sandbox profile for worker processes. The profile must constrain worktree/filesystem access, network, process tree, CPU, memory, disk, timeout, cancellation, and cleanup, and refuse admission when required controls are unavailable. Use an OS-native mechanism or a clearly bounded helper, deterministic fake tests, hostile escape/symlink/network/process cases, and explicit unsupported results. Never label the current subprocess adapter as production sandboxing.
