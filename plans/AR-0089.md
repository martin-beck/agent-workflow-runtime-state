# AR-0089 generic agent-workflow project integration

Implement the provider-neutral project integration contract for registration, task intake, capability declarations, artifact exchange, evidence handoff, record/replay, and terminal reconciliation. Use a local generic project fake and deterministic response library with explicit failure semantics. Consuming projects may provide example workloads and evidence, but are not runtime components or runtime authorities.
