# AR-0084 agent registry and capability preflight

Create a versioned registry and preflight engine for agent profiles. Distinguish catalog presence, configuration acceptance, adapter capability, local mock qualification, and real-provider verification. Require exact profile digest, protocol version, resource declaration, supported lifecycle, and policy compatibility. Test all three named profiles plus generic mocks, unsupported capabilities, stale profiles, and contradictory declarations.
