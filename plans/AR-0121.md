Implement provider-free HTTP/IPC-shaped authority client interfaces for Coordinator, AWQ, AWG, and UI. Validate protocol/version, task revision, correlation, signatures or authenticated transport metadata where available, bounded deadlines, retries, unknown write outcomes, and privacy-safe projections. Require every executable workflow to use the four adapters in order. CI must exercise deterministic fake servers and hostile stale, replay, timeout, crossed-authority, and ambiguous-outcome cases. Live hosted consumption is separately reported and never implied by fake-server tests.
