# AR-0070 durable journal and recovery engine

Implement append-only journal segments or an equivalent transactional local
store, checkpoint manifests, recovery scanning, fence validation, compaction
boundaries, and terminal reconciliation. Test process interruption at every
write phase, duplicate operation replay, stale lease mutation, corrupted
records, unknown schema versions, and privacy-bearing payloads.
