From a clean checkout and exact umbrella release, bootstrap a new complex software project, register at least two heterogeneous agents, negotiate capabilities, schedule concurrent bounded work, route every quality/guidance/UI decision through mandatory gates, execute local LLM-mock sessions, record/replay and compare results, recover a worker, reconcile evidence/accounting, and verify release/rollback. Require exact-head CI, TLA/TLC, hostile isolation tests, privacy/security checks, and explicit optional-provider separation. Accept only when no workflow step can bypass authority or silently fall back to an unverified control.
