Replace local-only lifecycle assumptions at the executable boundary with a Coordinator client seam that reads the exact task revision, claims the task, acquires/fences a worker lease, heartbeats, writes session events, and reconciles terminal state. Keep Coordinator authoritative: runtime may observe and request transitions but cannot manufacture claims, acceptance, or completion. Use a deterministic durable fake for CI with CAS conflicts, duplicate operation IDs, stale leases, expiry, ambiguous writes, and restart recovery. Hosted transport remains optional; the same contract must be used by local and hosted implementations.
