Add a provider-neutral request/response and streaming event protocol compatible with a local LLM mock service/library such as LiteLLM-style interfaces. Implement deterministic mock responses, cancellation, bounded tokens/latency, recording with privacy-safe digests, replay, mismatch detection, and two-agent comparison from the same benchmark input. Route all sessions through registry, authority gates, scheduler, sandbox, evidence, and accounting; never require a live provider.
