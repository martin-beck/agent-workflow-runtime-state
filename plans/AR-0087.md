# AR-0087 multi-project isolation and artifact routing

Implement project registry, revision/worktree allocation, artifact namespaces, dependency inputs, retention, cleanup, and cross-project denial. Use disposable local Git fixtures, concurrent mock sessions, collisions, stale worktrees, interrupted cleanup, and digest mismatch tests. No project checkout outside the test fixture and no remote publication are required.
