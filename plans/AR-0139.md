Inject process crashes, host restart, lease expiry, lost output, duplicate/reordered frames, authority timeouts, ambiguous writes, UI interruption, sandbox denial, disk/output/resource exhaustion, and concurrent project contention into a real autonomous run. Verify fencing, no duplicate accepted work, no orphan processes, durable recovery, user escalation when repair fails, privacy-safe evidence, and deterministic final reconciliation. Require repeated runs and bounded cleanup checks.
