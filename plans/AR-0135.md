Wire every material worker action through the owning control path: AWQ evidence submission and gate consumption, AWG uncertainty/decision requests, and revision-bound UI human decisions. A worker must be unable to alter specifications, tests, user decisions, or acceptance evidence directly. A blocked worker that repair workers cannot unblock must stop and trigger AWG/UI. Test skipped gates, changed specs/tests, ambiguous decisions, stale UI responses, rejected quality, user cancellation, and repair escalation with deterministic authority fixtures.
Provider assumption: agent configuration is an input to admission and is not changed by workers or runtime.
