# AR-0080 production integration baseline and compatibility matrix

Analyze the existing offline specifications and map every required executable boundary to its owning authority, version, capability, failure mode, and evidence class. Add a machine-readable compatibility matrix for host profiles, project classes, runtime versions, agent profiles, authority bridges, and local-mock modes. Define admission, unsupported, degraded, and blocked outcomes. Add schema/fixture/checker tests and hostile cases for unknown versions, crossed revisions, unsupported combinations, and provider claims without evidence. Local-only; no remote service or LLM connection.
