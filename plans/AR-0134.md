Implement a supervisor monitor that tracks process liveness, heartbeats, output progress, resource usage, lease expiry, cancellation requests, cancellation acknowledgement, and terminal reconciliation. It must kill the complete process group, prevent orphan workers, fence late output, retry only according to durable job policy, and recover a crashed worker with a new lease and checkpoint. Test signal races, hung workers, output stalls, process descendants, host restart, stale heartbeats, duplicate terminal events, and unknown cancellation outcomes.
Provider assumption: the agent process is already configured; monitoring controls the process and session, never credentials or model selection.
