# AR-0085 multi-agent executable adapter sessions

Compose AR-0083 transport, AR-0084 profiles, and AR-0073 framing into executable sessions. Implement normalized request/stream/interruption/resume/close events, bounded payloads, correlation, fencing, and explicit provider-error/unsupported outcomes. The test adapter is a local deterministic process and its model backend is an in-process LiteLLM-style mock; never invoke a provider binary or external LLM.
