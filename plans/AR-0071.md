# AR-0071 executable scheduler service

Adapt the proven scheduler reference model into an operational local service
with explicit command/event APIs. Preserve the AR-0062 invariants rather than
creating a second scheduling policy. Test multiple workers and tenants with a
virtual clock, then inject crashes between reservation, dispatch, heartbeat,
completion, and journal commit.
