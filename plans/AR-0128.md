Implement an atomic, revisioned agent registry with adapter identity, command/profile, capabilities, resource requirements, supported lifecycle operations, sandbox requirement, and deterministic health/preflight results. Integrate registry admission with scheduler and AgentSession so unsupported capabilities, duplicate identities, stale profiles, and unsafe commands fail closed. Provide at least two heterogeneous deterministic fake adapters and concurrent tests; no backend provider or credentials are required.
