# AR-0082 concurrent lease and worker coordination

Build shared lease acquisition, renewal, expiry, fencing, handoff, and recovery over AR-0081. Model N workers and delayed/reordered messages with deterministic scheduling. Prove a single fenced owner, monotonic tokens, bounded heartbeat, no double completion, retry idempotence, and safe recovery after ambiguous writes. Add property-style interleaving tests, minimized counterexamples, and accounting for lease transitions.
